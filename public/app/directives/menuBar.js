angular.module('app').directive('menuBar', function($parse) {
    return {
        restrict: 'EA',
        link: function(scope, element, attrs) {

        },
        controller: function($scope){
          
        } //link closing
    }; //return closing
}); //closing
